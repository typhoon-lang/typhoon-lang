namespace std::result

enum Result<T, E> {
  Ok(T),
  Err(E),
}

//extend<T, E> Result<T, E> {
//  fn map<U>(self, f: T -> U) -> Result<U, E> {
//    match self {
//      Ok(v) => Ok(f(v)),
//      y => y,
//    }
//  }

//  fn map_err<R>(self, f: E -> R) -> Result<T, R> {
//    match self {
//      Err(v) => Err(f(v)),
//      y => y,
//    }
//  }
//}
